clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      token:
        from_secret: github_token

when:
  branch: main
  event: [push, pull_request]

steps:
  - name: install
    image: node:22-alpine
    commands:
      - corepack enable
      - corepack prepare pnpm@latest --activate
      - pnpm -C app install --frozen-lockfile
      - pnpm -C app db:generate

  - name: lint
    image: node:22-alpine
    commands:
      - corepack enable
      - corepack prepare pnpm@latest --activate
      - pnpm -C app lint

  - name: typecheck
    image: node:22-alpine
    commands:
      - corepack enable
      - corepack prepare pnpm@latest --activate
      - pnpm -C app typecheck

  - name: build
    image: node:22-alpine
    commands:
      - corepack enable
      - corepack prepare pnpm@latest --activate
      - pnpm -C app build

  - name: docker-build-push
    image: woodpeckerci/plugin-docker-buildx
    when:
      event: push
    settings:
      repo: link108/game-theory-sim
      tags:
        - ${CI_COMMIT_SHA:0:7}
        - latest
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password

  - name: deploy-to-homelab
    image: alpine:3.19
    when:
      event: push
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - apk add --no-cache git yq github-cli
      - SHORT_SHA=$(echo $CI_COMMIT_SHA | cut -c1-7)
      - "git clone https://x-access-token:$GITHUB_TOKEN@github.com/link108/homelab.git /tmp/homelab"
      - cd /tmp/homelab
      - "git config user.email woodpecker@ci.byah.org"
      - "git config user.name Woodpecker CI"
      - git checkout -b deploy/game-theory-sim-$SHORT_SHA
      - ./scripts/request-deploy.sh game-theory-sim $SHORT_SHA
      - git add -A
      - 'git commit -m "deploy(game-theory-sim): $SHORT_SHA"'
      - git push -u origin deploy/game-theory-sim-$SHORT_SHA
      - 'gh pr create --repo link108/homelab --title "deploy(game-theory-sim): $SHORT_SHA" --body "Auto-deploy from game-theory-sim CI pipeline" --head deploy/game-theory-sim-$SHORT_SHA'

  - name: auto-merge
    image: alpine:3.19
    when:
      event: pull_request
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - apk add --no-cache github-cli jq
      - |
        # Check if PR is draft or missing automerge label
        PR_DATA=$(gh pr view $CI_COMMIT_PULL_REQUEST --repo $CI_REPO --json isDraft,labels)
        IS_DRAFT=$(echo "$PR_DATA" | jq -r '.isDraft')
        HAS_LABEL=$(echo "$PR_DATA" | jq -r '.labels[]?.name // empty' | grep -q "automerge" && echo "true" || echo "false")

        if [ "$IS_DRAFT" = "true" ]; then
          echo "Skipping auto-merge: PR is a draft"
          exit 0
        fi

        if [ "$HAS_LABEL" != "true" ]; then
          echo "Skipping auto-merge: PR missing 'automerge' label"
          exit 0
        fi

        echo "Auto-merging PR #$CI_COMMIT_PULL_REQUEST"
        gh pr merge $CI_COMMIT_PULL_REQUEST \
          --repo $CI_REPO \
          --squash \
          --delete-branch \
          --match-head-commit $CI_COMMIT_SHA

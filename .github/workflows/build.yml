name: Build, test, and deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  IMAGE: link108/game-theory-sim

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Backend checks
        working-directory: backend
        run: |
          uv sync
          uv run ruff check .
          uv run pytest

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Frontend build
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Compute short SHA
        if: github.event_name == 'push'
        id: sha
        run: echo "short=$(echo "${{ github.sha }}" | cut -c1-7)" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        if: github.event_name == 'push'
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            APPLICATION_VERSION=0.1.0
            GIT_SHA=${{ github.sha }}
          tags: |
            ${{ env.IMAGE }}:${{ steps.sha.outputs.short }}
            ${{ env.IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Open deploy PR against homelab
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.HOMELAB_PUSH_TOKEN }}
        run: |
          SHORT_SHA="${{ steps.sha.outputs.short }}"
          git clone "https://x-access-token:${GH_TOKEN}@github.com/link108/homelab.git" /tmp/homelab
          cd /tmp/homelab
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions (game-theory-sim)"
          git checkout -b "deploy/game-theory-sim-$SHORT_SHA"
          ./scripts/request-deploy.sh game-theory-sim "$SHORT_SHA"
          git add -A
          git commit -m "deploy(game-theory-sim): $SHORT_SHA"
          git push -u origin "deploy/game-theory-sim-$SHORT_SHA"
          gh pr create --repo link108/homelab \
            --title "deploy(game-theory-sim): $SHORT_SHA" \
            --body "Auto-deploy from game-theory-sim GitHub Actions" \
            --head "deploy/game-theory-sim-$SHORT_SHA"

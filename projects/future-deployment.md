# Future Project: Deployment

## Goal
Deploy to Hetzner VPS with Docker, nginx, and systemd.

## Notes
- Postgres runs locally on the Hetzner host
- Next.js runs as a Node process (or Docker container)
- nginx reverse proxy for HTTPS
- To be scoped once core app is functional
